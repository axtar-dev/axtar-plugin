{
  "description": "Axtar hooks (codex) — gate code edits via the engine /evaluate endpoint.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "apply_patch|Bash|mcp__.*__(write|edit|create|delete|move).*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/dist/hosts/codex/entrypoints/pre.js",
            "timeout": 2
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "apply_patch|Bash|mcp__.*__(write|edit|create|delete|move).*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/dist/hosts/codex/entrypoints/post.js",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
