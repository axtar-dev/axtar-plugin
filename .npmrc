# Make `npm version` create tags in Claude Code's plugin-release format,
# `axtar--v<version>` (default is `v<version>`). This is what the Release
# workflow triggers on and what `/plugin update` treats as a release.
tag-version-prefix=axtar--v
