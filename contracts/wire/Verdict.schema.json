{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Verdict",
  "description": "Three-valued verdict computed by `compute_verdict` over a list of Violations. block > warn > pass; suggestion-severity violations never elevate the verdict (see PROJECT-PAPER.md \u00a76 and D-006).",
  "type": "string",
  "enum": [
    "block",
    "warn",
    "pass"
  ]
}
